// Should already be required, here for clarity
require('angular');

// Now load auth interceptor module.
require('./http-auth-interceptor');

// Export namespace
module.exports = 'http-auth-interceptor';
